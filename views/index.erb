<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>colorpicker</title>
    <style>
    body{
        background-color: #fff;
        background-image: url("/back.jpg");
        background-repeat: repeat-x;
    }
    img{
        margin-top: 20px;
        margin-bottom: 10px;
    }
    .container{
        margin: 0 auto;
        width: 1000px;
        height: 100%;
        background-color: #fff;
    }
    .upload{
        background-color: #fff;
        width: 1050px;
        height: 75px;
        border-radius: 10px;
        border: solid 5px #eee;
        padding-bottom: 10px;
        display: flex;
        justify-content: space-between;
        text-align: center;
    }
    .thumbnail{
        background-color: #fff;
        width: 1000px;
        height: 600px;
        margin-top: 10px;
        border-radius: 5px;
        padding-top: 20px;
        
        display: flex;
        flex-wrap: wrap;
    }
    form{
        width="800"
    }
    .a{
        float: left;
    }
    .b{
        float: right;
    }
    .btn{
        width: 300px;
        height: 50px;
        border-radius: 5px;
        background-color: #FFF;
        margin-left:150px;
    }
    .btn2{
        width: 200px;
        height: 50px;
        border-radius: 5px;
        background-color:#FFF;
    }
    .image-box{
        width: 200px;
        height: 200px;
        margin-left:50px;
    }
    .image{
        width: 200px;
        height: 180px;
    }
    </style>
</head>
<body>
    <div class="container">
        <img src="/logo.png">
        <div class="upload">
            <form action="/new" method="post" enctype="multipart/form-data">
                <p class="a"><input type="file" name="file" class="btn"></p>
                <p class="b"><input type="submit" value="アップロード" class="btn"></p>
            </form>
        </div>
        <div class="thumbnail">
            <% @contents.each do |content| %>
                <div class="image-box">
                    <div class="image">
                        <% unless content.img_url.empty? %>
                            <a href="/<%= content.id %>/pick" class="a"><img src="<%= content.img_url %>" width="200"></a>
                        <% end %>
                    </div>
                <span>
                    <form action="/delete/<%= content.id %>" method="post">
                        <input type="submit" value="削除" class="btn2">
                    </form>
                </span>
                </div>
            <% end %>
        </div>
    </div>
</body>
</html>